{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "react/no-unescaped-entities": "off",
    "react-hooks/rules-of-hooks": "off", // Disables the rules for hooks usage if needed
    "@typescript-eslint/no-explicit-any": "off", // Allows 'any' type
    "@typescript-eslint/no-empty-function": "off", // Allows empty functions if needed
    "@typescript-eslint/no-empty-interface": "off" // Allows empty interfaces if needed
  }
}
